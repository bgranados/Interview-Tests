This directory contains Cypress tests written in Typescript.

Install package.json in this directory for Cypress and Typescript by running
`npm install`

Run tests in this directory by running the command
`npx cypress open` for the GUI
or
`npx cypress run` for command line